# =============================================================================
# TimeAM API – Server Configuration
# =============================================================================

NODE_ENV=development
PORT=3000

# =============================================================================
# Firebase Admin SDK
# =============================================================================
# WICHTIG: Service Account ist ein SECRET – niemals committen!
#
# Option 1: Pfad zur JSON-Datei (empfohlen für lokale Entwicklung)
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# Option 2: JSON als String (für Cloud Deployments)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key":"..."}

# Project ID
FIREBASE_PROJECT_ID=timeam-4f62c

# =============================================================================
# Optional: Firebase Emulator (für lokale Entwicklung ohne echtes Firebase)
# =============================================================================
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
# FIRESTORE_EMULATOR_HOST=localhost:8080

# =============================================================================
# Super-Admin (Developer/CEO) Konfiguration
# =============================================================================
# Komma-separierte Liste von Firebase Auth UIDs, die Super-Admin-Zugriff haben
# Diese User können alle Tenants verwalten und Module aktivieren/deaktivieren
# Beispiel: SUPER_ADMIN_UIDS=abc123,def456
SUPER_ADMIN_UIDS=

# =============================================================================
# Weitere Backend-Konfiguration
# =============================================================================
# Später: Datenbank, Redis, etc.
